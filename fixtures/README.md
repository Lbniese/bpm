# fixtures/

Reserved for fixture projects used by benchmarks and differential/fixture
medium application fixtures with real `package-lock.json` files.

Not populated yet. Current tests build fixtures in-memory (see
`tests/common/mod.rs`) rather than depending on checked-in project trees.
